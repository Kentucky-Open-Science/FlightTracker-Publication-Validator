{
    "name": "Flight Tracker Publication Validator",
    "description": "A tool which creates surveys linking service requests with possible publications impacted, and then allows for verification from a researcher.",
    "namespace": "UKModules\\PublicationValidator",
    "framework-version": 16,
    "authors": [
        {
            "name": "Noah Perry",
            "email": "noah.perry@uky.edu",
            "institution": "University of Kentucky - CCTS"
        }
    ],
    "links": {
        "project": [
            {
                "name": "Export Responses as CSV",
                "icon": "fas fa-file-export",
                "url": "pages/custom_export.php",
                "show-header-and-footer": true
            }
        ]
    },
    "project-settings": [
        {
            "key": "validation_form",
            "name": "Input the Instrument name for the validation form.",
            "required": true,
            "type": "text"
        },
        {
            "key": "api-key-desc",
            "name": "Please provide the API key for a cohort of publications from Flight Tracker. Press the '+' to add another.",
            "required": true,
            "type": "sub_settings",
            "repeatable": true,
            "sub_settings": [
                {
                    "key": "cohort-api-project-name",
                    "name": "Cohort Name",
                    "required": false,
                    "type": "text"
                },
                {
                    "key": "cohort-api-key",
                    "name": "Cohort API Key",
                    "required": false,
                    "type": "text",
                    "repeatable": false
                }
            ]
        }
    ],
    "no-auth-pages": [
        "custom_export"
    ],
    "include-authors-in-api-info": true,
    "api-actions": {
        "get-citations-by-userid": {
            "description": "Select all citations belonging to a certain user by matching their UserID.",
            "access": ["auth"]
        }
    }
}
